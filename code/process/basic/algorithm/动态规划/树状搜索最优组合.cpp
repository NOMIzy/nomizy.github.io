#include<iostream>
using namespace std;

int maxV=0
void Backtracking(int n,int A[n],int B[n],int C[n],int V[n],int now){
    //遍历到底 不满足添加条件 
    if(){
        //记录更大的maxV值
    }
    //对于第now个建筑，或者建，或者不建，生成两个子节点，分别计算其总价值
    //不建就不变
    //建的话检查是否能建
    for(){}
}
int main(){
    int n;
    cin>>n;
    int A[n],B[n],C[n],V[n];
    for(int i=0;i<n;i++){
        cin>>A[i]>>B[i]>>C[i]>>V[i];
    }

}